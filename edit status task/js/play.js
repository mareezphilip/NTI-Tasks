// data = [{name:"marwa", age:38}, {name:"adam", age:9}]
// localStorage.setItem("data", JSON.stringify(data))

const storeToLocalStorage = function(key, data){
    //if there is an error set item with []
    try{
        console.log(test)
    }
    catch(e){
        console.log(e.message)
    }
}

storeToLocalStorage("","")

